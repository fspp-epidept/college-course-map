# Hugging Face credentials for upload.py (not yet implemented).
# Copy this file to `.env` (which is gitignored) and fill in values.
# uv automatically loads .env from this directory when invoking scripts via `uv run`.

HF_USERNAME=
HF_TOKEN=

# Optional: override path to validation.csv (defaults to <repo_root>/data/validation.csv)
# COURSE_CLASSIFIER_VALIDATION_CSV=
